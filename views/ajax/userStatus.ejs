<% if(!currentUser){%>
  <!-- <li class="nav-item"> -->
    <a style="cursor: pointer; font-family: 'Montserrat', sans-serif;" data-toggle="modal" data-target="#loginModal" class="nav-link menu">
                 Login
    </a>
  </li>
          <%} else {%>

            <li class="nav-item dropdown menu " id="dropdown-edit">
              <a class="nav-link dropdown-toggle menu" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <% if(!currentUser){ %> &nbsp Login <%} else {%> My Account <% } %>
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <% if(!currentUser){ %>
                  <a class="dropdown-item" href="/login"><i class="fa fa-user" aria-hidden="true"></i> &nbsp Your Account</a>
                <%} else {%>
                  <a class="dropdown-item" href="/users/<%=currentUser.id%>"><i style="color: #17a2b8;" class="fas fa-user-circle" aria-hidden="true"></i> &nbsp Profile</a>
                  <div class="dropdown-divider"></div>
                  <a class="dropdown-item" href="/orders/<%=currentUser.id%>"><i style="color: #17a2b8;" class="fas fa-folder"></i>&nbsp Orders</a>
                <% } %>
                
                <div class="dropdown-divider"></div>

                  <% if(!currentUser){ %>
                  <div class="container">
                    <button type="button" onclick="location.href='/login'" class="btn btn-primary btn-sm">LOGIN</button>

                    <button type="button" onclick="location.href='/register'" class="btn btn-primary btn-sm ml-3">Sign Up</button>
                  </div>
                <%} else {%>
                  <a href="/logout" class="dropdown-item" style="cursor: pointer;"><i style="color: #17a2b8;" class="fas fa-sign-out-alt"></i>&nbsp Logout</a>
                <% } %>
                 
              </div>
            <!-- </li> -->
          <%}%>